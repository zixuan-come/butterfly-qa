# 项目协作约定

## 项目目标

本项目实现一个基于 Codex 的测试全流程 Agent 原型。当前优先验证流程闭环和产物质量，不提前扩展到接口自动化、Web 后台或复杂知识库。

## 目录边界

- `src/qa_agent/`：流程、Schema、存储和校验等可测试的 Python 代码。
- `skills/`：具体工作能力的 Skill 定义，只描述职责、输入、步骤、输出和约束。
- `agents/`：Agent 角色契约和调用边界，不在其中堆积业务实现。
- `projects/`：示例项目和运行时产物。
- `tests/`：不依赖真实大模型的单元测试和流程测试。
- `docs/`：需求、设计、开发和交接文档。

## 工程规则

- 结构化产物优先使用 Pydantic 模型和 JSON 序列化。
- 状态转换必须显式、可记录、可恢复。
- 不覆盖原始输入和历史版本。
- 不自动修改正式需求，不绕过人工审批。
- 任何不确定结论都必须标记为待确认或假设。
- 新增能力前先判断是否可以通过已有 Skill 解决，避免无必要拆分 Agent。
- 不把真实凭证、生产数据或敏感信息提交到项目目录。
